web:
  name: demo-web
  image: <your-internal-registry>/ocpdemoapp:latest
  port: 8080
  replicas: 1

mongodb:
  name: mongodb
  image: <your-internal-registry>/mongodb:latest
  port: 27017
  replicas: 1
  database: demodb
  user: demo
  password: demo
  rootPassword: rootpassword

route:
  enabled: true
  tls: true
